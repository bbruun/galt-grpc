syntax = "proto3";
package server;
option go_package = "github.com/bbruun/galt/server";

message Register {
    string MachineName = 1;
    string PublicKey = 2;
    string ServerResponse = 3;
}

service RegisterClientService {
    rpc NewRegistratoin(Register) returns (Register) {}
}